name: Déployer Nexora V524

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    name: Installer la V524
    runs-on: ubuntu-latest

    steps:
      - name: Récupérer le dépôt
        uses: actions/checkout@v4

      - name: Vérifier et installer la V524 à la racine
        shell: bash
        run: |
          set -euo pipefail

          archive="NEXORA_V524_FINAL_COMPLET_PRET_POUR_VERCEL.zip"
          extract_dir="$RUNNER_TEMP/nexora-v524"

          if [[ ! -f "$archive" ]]; then
            echo "Le ZIP V524 est introuvable à la racine du dépôt."
            exit 1
          fi

          mkdir -p "$extract_dir"
          unzip -q "$archive" -d "$extract_dir"

          test -f "$extract_dir/index.html"
          test -f "$extract_dir/vercel.json"
          test -f "$extract_dir/version.json"
          grep -q '"version"[[:space:]]*:[[:space:]]*"V524"' "$extract_dir/version.json"

          find . -mindepth 1 -maxdepth 1 \
            ! -name '.git' \
            ! -name '.github' \
            ! -name "$archive" \
            -exec rm -rf -- {} +

          cp -a "$extract_dir"/. .
          rm -f "$archive"

      - name: Enregistrer la V524 dans la branche principale
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A

          if git diff --cached --quiet; then
            echo "La V524 est déjà installée."
          else
            git commit -m "Déployer Nexora V524 final"
            git push
          fi
