name: Integrer Sciences CP1 V604
on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/migrate_cp1_science_v604.py'
      - '.github/workflows/migrate-cp1-science-v604.yml'
permissions:
  contents: write
jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python .github/scripts/migrate_cp1_science_v604.py
      - run: node --check assets/js/nx-v157-primary-school-script.js
      - run: |
          git config user.name 'Nexora Curriculum Bot'
          git config user.email 'actions@github.com'
          git add assets/js/nx-v157-primary-school-script.js version.json
          if git diff --cached --quiet; then echo 'Aucun changement'; else git commit -m 'Complete CP1 science with 20 illustrated audio lessons V604' && git push; fi
